{
  "name": "activity-heatmap-card",
  "version": "0.1.0",
  "private": true,
  "description": "Home Assistant Lovelace card: a GitHub-style contribution heatmap of any entity's daily history",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.dev.ts",
    "build": "vite build --config vite.config.build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
